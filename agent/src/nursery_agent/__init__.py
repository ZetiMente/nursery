"""nursery_agent — the code that runs inside a Nursery agent container.

Phase 2a scope: load spec, load soul, start an HTTP server that reports
health and echoes inbound messages. No real inference yet.
"""

__version__ = "0.1.0"
